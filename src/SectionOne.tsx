import 'twin.macro';
import DerivedCard from './DerivedCard';

type Props = {};

const SectionOne = ({}: Props) => {
  return (
    <div>
      <DerivedCard />
    </div>
  );
};

export default SectionOne;
